let num = 2;
let sum = 2 + 2;
let result = num + sum; // 6

// умножение
let n = 2 * 2;

// деление
let division = 2 / 2;

// возведение в степень
let a = 2 ** 3; // 8

// получение остатка от деления
let b = 5 % 2; // 1
let c = 100 % 2; // 0

//  дробные числа
let d = 0.5;

// деление на ноль
let m = 987 / -0; // -Infinity
let k = 987 / 0; // Infinity

let z = (0.2 / 0.2) * 0.00003568277083;

let number1 = 5;
// let sum = 5 + 2;
number1 = number1 + 2;
number1 += 2;
let number2 = 5;
number2 = number2 * 2;
number2 *= 2;

/*
let num = 1;
// postfix
console.log("num", num); // 1
console.log("++num", ++num); // ++
console.log("num", num); // 2

// prefix
++num;
--num;

let a = "Никита";
let b = 10;
// Никита - 10 очков !

console.log(a + " - " + b + " points !");
const str = `${a} - ${b ** b * 2} points !`;

console.log(str);
*/

// Falsy values !!!

// false
// 0
// ""
// NaN
// null
// undefined

// явное
// строка в число
const number = "5.05";
let a = +number; // number 5.05
let b = Number(number); //  5.05
let c = parseInt(number); // 5


/*
// число в строку
let c = 13..toString(); // 1
let d = 13;
let e = d.toString(); // 2
let f = String("13") // 3
let x = parseInt("43xxx") // 4 -> 43

// в Boolean
let g = ""
let n = 234;
let m = null;

Boolean(g) // false
Boolean(n) // true
Boolean(m) // false
*/
